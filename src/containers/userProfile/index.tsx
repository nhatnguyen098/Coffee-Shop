import React from 'react'

const Index = ({match,history}:any) => {
    // console.log(match,history)
    return (
        <div>
            hello user profile
        </div>
    )
}

export default Index
